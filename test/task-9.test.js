const assert = require('chai').assert;

// 9. Task 9

describe(`Testing Task-9 ... 'countDown()' \n
  Write function countDown. Function expects number and logs values one by one till zero with one second delay.
  `, () => {

  it(`Async, untestable`, () => {
    assert.equal(true, true);
  });
});
